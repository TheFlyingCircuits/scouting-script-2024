[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/majora"]

[project]
version = "1.0.0"
name = "majora"
requires-python = ">=3.11"
dependencies = ["polars"]
